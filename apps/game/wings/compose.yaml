services:
  wings:
    image: ghcr.io/pterodactyl/wings:latest@sha256:97937976fdb0ea0ac78797d8c86311ef80937f23f186a75a6903aff4540487d1
    container_name: wings
    restart: unless-stopped
    ports:
      - 8080:8080
      - 2022:2022
      - 443:443
    tty: true
    environment:
      - TZ=Asia/Yekaterinburg
      - WINGS_UID=988
      - WINGS_GID=988
      - WINGS_USERNAME=pterodactyl
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /var/lib/docker/containers/:/var/lib/docker/containers/
      - ${DATA_ROOT:-./data}/:/etc/pterodactyl/
      - ${DATA_ROOT:-./data}/:/var/lib/pterodactyl/
      - ${DATA_ROOT:-./data}/log/:/var/log/pterodactyl/
      - ${DATA_ROOT:-./data}/tmp/:/tmp/pterodactyl/
      - /etc/ssl/certs:/etc/ssl/certs:ro
      # you may need /srv/daemon-data if you are upgrading from an old daemon
      #- "/srv/daemon-data/:/srv/daemon-data/"
      # Required for ssl if you use let's encrypt. uncomment to use.
      #- "/etc/letsencrypt/:/etc/letsencrypt/"
    networks:
      - backend

networks:
  backend:
    external: true